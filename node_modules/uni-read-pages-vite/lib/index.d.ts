import { Config } from './interfaces/index';
declare class TransformPages {
    private CONFIG;
    private pagesPath;
    private uniPagesJSON;
    routes: Record<string, any>[];
    private platform;
    /**
     * 构造函数
     * @param config 保留字段配置
     * @param pagesPath pages.json 文件相对与项目根目录的位置
     */
    constructor(pagesPath?: string, config?: Config);
    /**
     * 获取所有pages.json下的内容 返回json
     */
    get pagesJson(): any;
    /**
     * 通过读取pages.json文件 生成直接可用的routes
     */
    getPagesRoutes(pages?: any, rootPath?: string | null): Record<string, any>[];
    /**
     * 解析小程序分包路径
     */
    getNotMpRoutes(): any[];
}
export default TransformPages;
