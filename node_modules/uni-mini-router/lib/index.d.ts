import type { Router, RouterOptions } from './interfaces/index';
/**
 * Creates a Router instance that can be used by a Vue app.
 *
 */
export declare function createRouter(options: RouterOptions): Router;
export * from './core';
