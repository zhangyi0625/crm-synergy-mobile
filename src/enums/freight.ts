/**
 * @description: 舱位类型
 */

export enum ProductType {
	QMS = "QMS",
	OTHER = "OTHER",
}

export enum priceType {
	COST = "costPrice",
	INNER = "innerPrice",
	OUTER = "outerPrice"
}

export enum FreightCostDetail {

}