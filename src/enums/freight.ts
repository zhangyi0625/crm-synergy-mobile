/**
 * @description: 舱位类型
 */

export enum ProductType {
	QMS = "QMS",
	ZDP = "ZDP",
	SPOT = "SPOT"
}

export enum priceType {
	COST = "costPrice",
	INNER = "innerPrice",
	OUTER = "outerPrice"
}

export enum FreightCostDetail {

}